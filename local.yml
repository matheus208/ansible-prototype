---
- hosts: localhost
  connection: local
  become: no
  tasks:
  - name: install htop
    package:
      name: htop
  - name: install openvpn
    package:
      name: openvpn
  - name: install git
    package:
      name: git
      
